import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
    },
    input: {
        borderBottomWidth: 1,
        borderColor: "white",
        color: "white",
        padding: 10,
    },
});