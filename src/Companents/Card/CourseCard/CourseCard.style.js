import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        height: 200,
        margin: 10,
        borderRadius: 10,
        borderWidth: 2,
        justifyContent:"center",
        alignItems: "center",
        borderColor: "#EBEFF1",
    },
    title: {
        color: "#FE6F00",
        fontSize: 22,
    },
});